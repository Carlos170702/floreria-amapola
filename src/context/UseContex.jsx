// Importar el método 'createContext' desde React
import { createContext } from "react";

// Crear un objeto de contexto utilizando el método 'createContext'
export const UseContex = createContext();
